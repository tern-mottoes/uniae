# Uniae — Usage Guide

**Uniae** is a read-only tool designed to interact with **Base networks**, including **Base Mainnet** and **Base Sepolia**, for querying blockchain data without modifying state. This document explains how to set up and use **Uniae**.

---

## Prerequisites

1. **Node.js** (version 14 or higher)
2. **npm** (Node Package Manager) or **yarn** (optional)

Ensure you have these dependencies installed before proceeding.

---

## Installation

To install **Uniae**, run the following command to install the necessary dependencies.

### Using npm:
```bash
npm install
